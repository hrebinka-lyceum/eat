# Локальні змінні. Справжні значення — у .env (він у .gitignore),
# на GitHub Pages — у секретах репозиторію.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon|publishable key>
